package model;

/**
 * A record for store players personal data.
 * @param userId represents the id of the player
 * @param userName represents the username of the player
 */
public record User(int userId, String userName) {}
